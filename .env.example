# ShanHai 环境变量示例（见 ADR 0011）
#
# 用法：复制为 .env 并填入真实密钥；.env 已被 gitignore，禁止提交。
# 未配置任何密钥时，Model Router 默认走 MockProvider，全部测试仍可离线运行。

# DeepSeek（https://platform.deepseek.com）
SHANHAI_DEEPSEEK_API_KEY=

# 真实 LLM 联调显式开关：设为 1 时才会发起真实 API 请求；默认离线。
SHANHAI_LIVE_LLM=
