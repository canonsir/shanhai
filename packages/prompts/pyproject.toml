[project]
name = "shanhai-prompts"
version = "0.1.0"
description = "ShanHai 提示词资产"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["shanhai_prompts"]
